const solution = (N, stages) => {
  const fail = {};

  stages.forEach((el) => {});

  const answer = [];

  return answer; // 실패율이 높은 스테이지부터 내림차순으로
};

const foo = 1; // 전체 스테이지 개수
const bar = []; // 막혀있는 스테이지번호

console.log(solution(foo, bar));
